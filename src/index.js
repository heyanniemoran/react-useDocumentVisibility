import React from 'react';
import ReactDOM from 'react-dom/client';
import LeaveTabCounter from './Main'

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <LeaveTabCounter />
  </React.StrictMode>
);
